import os

path = "/"

# listdir() method is used to get all files and directories in the specific directory

dir_list = os.listdir(path)

print("The files and the directory in the ", path, "is ")

print(dir_list)
